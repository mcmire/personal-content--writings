---
title: How I Use Obsidian
tags:
  - obsidian
---
[Obsidian](https://obsidian.md) is the tool I use to keep digital notes — to jot down thoughts and ideas about life and work, plan and track side projects, write long-form content like this, and more.

In this post, I'll document how I use Obsidian, how I've tweaked it to fit my needs, and how that's evolved over time.

## How I used to use Obsidian

These days, I try to keep things simple. But it wasn't always that way.

When I first switched to Obsidian, I migrated a bunch of notes from [Simplenote](https://simplenote.com/). I began imposing structure: I created subdirectories for different kinds of notes, and I turned a part of my vault into a database using Dataview. Over time, %%each time I opened the app, I felt unsettled. I had made things worse:%% I felt like I was spending too much energy thinking about how to organize my vault. I was feeding the %%obsessive%% perfectionist side of my brain, and %%each time I opened the app,I%% %%it felt more and more dissatisfied%% along with it a growing dissatisfaction.

Meanwhile, one of my coworkers was using [Roam](https://roamresearch.com/). %%Here, top-down organization is viewed as limiting … This tool is different because … One of the core premises is that directories are a poor paradigm for organizing thoughts and ideas because they don't map to how your brain acquires knowledge in real life … Roam dispenses with top-down organization and … In Roam, thoughts and ideas are like nodes in a graph instead of a file in a filetree … Roam is a unique tool because it dispenses with files entirely … Roam offered a fresh perspective: instead of a hierarchy of files and folders, it views files and folders as an antiquated paradigm for organizing and managing information; instead, it%% Roam takes a unique position: thoughts, ideas, and other pieces of information %%captured over time%% are messy and don't fit cleanly into a hierarchy of folders. Instead, they're best represented by a graph which grows and changes shape organically as new information is added. [^1] [^2] %%it treats your ideas and thoughts as nodes on a knowledge graph%% %%they're, which grows organically as you connect nodes together.%% Adding another node to the graph should be effortless: each time you load the app, you start with an automatically generated note for the current day. This removes cognitive overhead and allows you to form consistent habits, thereby giving you a sense of "home". [^3]

%%These days I try to keep things simple. I still find directories useful, but%% %%I've tried to wean myself off of them slowly, and%% %%I've limited myself to three:%%

%%While I've stuck with Obsidian, I've used limitations to my advantage … the limitations imposed by Roam inspired me …  I started to bring these ideas into Obsidian … Inspired by these limitations, I disabled most of my plugins and narrowed down the list of …%% While I've continued to use Obsidian for practical and philosophical reasons — it's local-first, cross-platform, extensible, and it just feels pleasant to use — I've learned that limitations can be an advantage.

## How I use Obsidian now

### Directories and tags

I still find directories useful, but there are three broad categories of notes that I use on a daily basis:

- `Diary/` — Date-based. Most of these entries are conversations between different voices in my head. I'll let them say what they want to say and move on. I don't typically go into a lot of detail (good luck to whoever reads this in the future!).

- `Log/` — Date-based. Ostensibly a "development log", but really it's the opposite of `Diary/`. These entries tend to be more rounded out, and I tend to revisit them multiple times per day. If I read an interesting article about politics, life, or work, and it inspires me, I'll write out my thoughts. If I'm reading a book, I'll keep notes. %%If I have an idea for a new side project, I'll map it out.%%
  
  And if I'm actively working on a project, I'll "live blog" the experience, noting the problems I'm facing and the solutions as I attempt to solve them. I have found this workflow in particular to be incredibly valuable. For instance, if I put a project on hold and come back to it a year later, I can go back in my notes, see I was headed when I left off, and I can very easily pick up the thread again.

- `Notes/` — Non-date-based. Most of my old notes are here, and I have yet to migrate them to a better format. Even so, I still tend to use this category to hold information that I think deserves a bit more structure, I want to keep together in one place, or I feel like would be buried if I left it in a journal entry. Each note tends to center around a concept or theme. For instance, when I do research for big-ticket items (e.g., baby strollers, cars, etc.) or make plans for trips, I'll add a note here. There are also simple lists of things such as potential baby names, etc.

 Beyond this structure, I've recently begun using tags to link notes by broader themes. This is a practice I already follow when posting bookmarks to [Pinboard](https://pinboard.in/), and I already have a list of tags in my head that I frequently use, so tagging notes is a pretty easy transition. For instance, if I'm thinking about AI, I'll use the tag `#artificial-intelligence`; if I'm thinking about career goals or dreams, I'll use `#future-work`. I also have tags for `#philosophy` and `#physics` and `#music-analysis`.  %%The one gripe I have about tags is that if you use a hashtag in a page, Obsidian applies it to the whole note. But journal entries may involve multiple topics in a single day. so I'll use a tag above each section. When I search for that tag, Obsidian doesn't know to jump down to the section I've tagged.%%

### Core plugins

I use a pretty small set of core plugins:

- I use **Quick Switcher**, but I also use **Files**. Most of the time I know what I'm looking for and can jump to  it, but sometimes I just want to look at a file tree (and it's a better interface to perform actions like renames).
- I use **Command Palette**. I like the recent trend of adding this feature to different apps. It reminds me of Spotlight on my Mac.
- I use **Page Preview**. It's useful not to have to jump into a note to see what's inside it.
- I use **Search**. I'm not sure what I'd do without this :) 
- I don't use **Backlinks**, **Outgoing Links**, or **Graph View** (yet). I imagine these would be more useful if I were following Zettelkasten, but I don't. Maybe some day.
- I don't use **Canvas**. Maybe if I were doing intense research I might need it, but I tend to only use features that I can both on mobile and desktop (see Syncing below), so I haven't given this a go yet. It's cool that the JSON representation is opensource, though.
- I don't use **Bases**. I think the Obsidian team has done a great job developing this feature. It certainly looks appealing. But coming from my experience with Dataview, I'm wary of making my vault too complex again.
- I don't use **Daily Notes**. Obsidian only supports creating daily notes in one location, but as mentioned above, I want to have two date-based directories. (I don't use Templates for a similar reason.) I use QuickAdd instead (see below).
- I don't use **Outline** or **Properties View**. I don't usually have documents that are so long that I have trouble scrolling through them. And I don't need to see properties across all of my notes.
- I don't use **Sync**; I use my own solution (again, see below).
- I don't use **Web Viewer**. I usually have my browser open on either my laptop or phone, and I want to keep all of my open tabs together in the same app.
- I don't use **Word Count**. I don't really care how long my notes are, and for blog posts like these, I write and rewrite thousands of words and hide the vast majority of them, so a word count doesn't really help anyway.

### Community plugins 

I try out new plugins from time to time, but here are the ones that have stuck:

- **Commander** + **QuickAdd** — I use the combination of these plugins to add a button to the ribbon that allows me to generate either a diary entry or a log entry using one of two templates I've set up.
- **Custom File Extensions** — This plugin allows me to edit MDX files (more on that later).
- **Outliner** — Provides some great quality-of-life features for working with lists. On my laptop, I can press `Cmd-Shift-Up` and `Cmd-Shift-Down` to move a list item up or down, respectively. On my phone, I have hotkeys to do the same thing.
- **Smart Typography** — This helps me write on my phone because I can type `--` to get an em-dash and `<-` or `->` to get ← and → respectively.
- **Vimrc Support** — I use Obsidian with Vim keybindings enabled. This plugin extends that by allowing me to see when I'm in insert mode vs. normal mode. It also allows me to add custom settings via a `.vimrc` file (for instance, I map `j` to `gj` and `k` to `gk` to be able to navigate through wrapped lines, and I use `set clipboard=unnamed` to use the system clipboard).

## Syncing Obsidian between devices 

I don't use Obsidian Sync. It definitely sounds useful, and I'm sure it's easy to set up. But I like to keep my Obsidian vault in Git so I have ultimate control of not only the current versions of the files in my vault, but also any historical versions as well. %%But I'd like to have control over every part of my data, and this includes historical versions of files.%%

Nor do I use the Obsidian Git plugin. As the README states, [it is very unstable on mobile](https://github.com/Vinzent03/obsidian-git?tab=readme-ov-file#-mobile-support-%EF%B8%8F--experimental). I often use Obsidian on my phone during the day, so that doesn't work for me.

Instead, I use a custom solution, which I will detail below.

### Preparing the vault repo

There are a couple of ways I've configured my Obsidian vault repo to work best with syncing.

I've instructed Git to ignore the Obsidian workspace JSON files (since they tend to collect conflicts easily) as well as trashed files:

```
.obsidian/workspace.json
.obsidian/workspace-mobile.json
.trash
```

I've also installed [Git LFS](https://git-lfs.com/). This way, if I want to add images or other media (such as audio files from the Audio Recorder plugin), I don't bloat the repo.

### Sync script

The work of syncing changes between the local and remote versions of my repo is done by a custom [shell script](https://gist.github.com/mcmire/33f5b5e1a7c96049424c17fb3060a4e0). (I keep in the vault repo under `./_scripts/sync.sh` so I can run it manually if I need to.). This script:

1. Uses a lockfile to ensure that only one instance of the script is running at a time
2. Ensures that the current branch is `main` up front
3. Runs the following steps, iterating through submodules first (see [below](#updating-this-site-via-obsidian) for why this is important):
	1. Ensures that `.md`, `.mdx`, and `.obsidian` files that have changes but have not been committed yet are staged
	2. Makes a new commit with the message "Automatic sync"
	3. Pulls new commits in, rebasing any pending commits on top of them
	4. Pushes the `main` branch
	5. Handles submodules (see below for why this is important).

Now, having a sync script is great, but I don't want to run it all the time. To emulate what Obsidian Sync does — and to make things work the same way across my laptop and phone — I have to employ a bit of ✨ magic ✨.

#### Desktop

On my Mac, I have two LaunchAgents set up:

- One runs a daemon which executes a wrapper around the sync script when any file within the Obsidian vault is changed. This wrapper performs debouncing, so that no syncing will occur while I'm actively editing a file, but only after I pause for 1 minute.
- The other runs the same script every day (in case I'm writing something and accidentally close my laptop before the script has a chance to run).

Both of these LaunchAgents are included in my [`dotfiles`](https://github.com/mcmire/dotfiles) repo. To install them, I simply clone this repo and run `bin/manage install`.

#### Mobile

On mobile, I use the [Automate](https://llamalab.com/automate/) app to mimic the same process I have on desktop. (As far as I know, this only works on Android; on iOS you could probably use [Shortcuts](https://support.apple.com/guide/shortcuts/welcome/9.0/ios).)

I have one workflow that looks like this:

![Automate workflow](./how-i-use-obsidian/automate-workflow.webp)

In short:

- When I switch away from Obsidian, or when I lock my phone, the sync script is executed.
- The sync script is also executed every 30 minutes automatically (in case either of the two checks above don't work properly).
- Like on desktop, executions of the sync script are debounced by 1 minute. If the timer is running and I switch back to Obsidian or unlock my phone, the timer is canceled.
- The workflow uses a plugin block, using [Termux:Tasker](https://wiki.termux.com/wiki/Termux:Tasker), to run the sync command. (It may be that I can use the "Shell script" action, but I haven't tested it out.)

The other workflow is used to simply run the sync script on demand.

To set this up, I followed these steps:

- I downloaded [F-Droid](https://f-droid.org/) and [Termux](https://termux.dev/) from the Play Store, and I downloaded Termux:Tasker and Automate from F-Droid.
- I opened Termux and ran `termux-setup-storage` to allow access to the SD card.
- I used `gh` to clone my Obsidian vault repo in `shared/storage`. I've found that `gh` works best over plain `git` because it handles authentication for private repos very smoothly.
- I created a symlink to the vault repo in the home directory for quicker access.
- I used `gh` to clone my `dotfiles` repo, also in the home directory. To install them, I first ran `pkg install ruby`, and then ran `bin/manage install`.

It's a bit more complicated than I want it to be, and I have to fiddle with it from time to time, but for the most part it works.

## Updating this site via Obsidian

One recent change I made was to integrate my site into my Obsidian vault.

As I explained in a [previous post](./behind-the-site.mdx#putting-it-all-together), the site you see right now — the layout and styles — lives in a separate repo from the content you're reading. I do this because if I ever want to change the way my site looks or even rebuild it using a different tech stack, I don't have to touch the content repo, and the Git history between the two will stay isolated.

Even with this setup, I found it more difficult to publish new blog posts than I liked. First, I had to be at my laptop to write a post. Then, I had to follow these steps:

1. Switch to my terminal
2. Navigate to my content repo, open it up in Neovim
3. Start writing
4. Realize that I want to see a preview
5. Open my content repo in VSCode, open up a Markdown preview
6. Remember to switch back to terminal and commit when I was at a stopping point
7. Finish the post, realize that I want to see what my post really looks like
8. Push up a branch
9. Make a pull request
10. See the post online

In contrast, Obsidian is so painless to use that I can open it anywhere, anytime, and it just works. It finally dawned on me: why can't I integrate my site and content repos into my Obsidian vault repo so that I can write blog posts the same exact way I would make a new journal entry or a note?

### Adding submodules

I needed some way of bringing all three repos under one umbrella. I debated using subtrees (or something similar, such as [git-subrepo](https://github.com/ingydotnet/git-subrepo)) to accomplish this, but both approaches require keeping some amount of history in the parent repo, and I didn't want that.

In the end I decided to use submodules. The hierarchy of my vault repo now looks like this:

```
obsidian-vault/
  _modules/
	  elliotwinkler.com/
		  modules/
			  personal-content--writings/
			...
	Diary/
	Log/
	Notes/
	...
```

That is, `.gitmodules` in the vault repo looks like this:

```
[submodule "_modules/elliotwinkler.com"]
        path = _modules/elliotwinkler.com
        url = https://github.com/mcmire/elliotwinkler.com
```

And `.gitmodules` in `_modules/elliotwinkler.com` looks like this:

```
[submodule "modules/personal-content--writings"]
        path = modules/personal-content--writings
        url = https://github.com/mcmire/personal-content--writings
```

### Updating the sync script 

Having integrated submodules, I now needed to make the sync script work with them. While updating the script, I noticed that if new changes are brought into a submodule via `git pull`, it will cause the parent repo to think that there are changes that need to be committed. To solve this problem, I have the script descend into submodules first, create any necessary commits (along with the usual pull and push steps), then proceed up the tree. Here's what a typical run of the script looks like:

```
[~/obsidian-vault/_modules/elliotwinkler.com/modules/personal-content--writings] Committing all files...
[~/obsidian-vault/_modules/elliotwinkler.com/modules/personal-content--writings] - Adding file: posts/how-i-use-obsidian.mdx
[~/obsidian-vault/_modules/elliotwinkler.com/modules/personal-content--writings] Fetching updates...
[~/obsidian-vault/_modules/elliotwinkler.com/modules/personal-content--writings] Pushing `main`...
[~/obsidian-vault/_modules/elliotwinkler.com/modules/personal-content--writings] Sync completed successfully!

[~/obsidian-vault/_modules/elliotwinkler.com] Committing all files...
[~/obsidian-vault/_modules/elliotwinkler.com] - Adding file: modules/personal-content--writings
[~/obsidian-vault/_modules/elliotwinkler.com] Fetching updates...
[~/obsidian-vault/_modules/elliotwinkler.com] Pushing `main`...
[~/obsidian-vault/_modules/elliotwinkler.com] Sync completed successfully!

[~/obsidian-vault] Committing all files...
[~/obsidian-vault] - Adding file: _modules/elliotwinkler.com
[~/obsidian-vault] Fetching updates...
[~/obsidian-vault] Pushing `main`...
[~/obsidian-vault] Sync completed successfully!
```

### Updating the deployment process

Besides submodules, I also updated how the site got deployed automatically.

I have always made it possible to deploy a preview build while writing a draft. This allows me to see what a post looks like on the site before it goes live. But previously, the way that I would manage these two environments worked like this:

- If I wanted to deploy my site to production, I could push a commit to `main`, either on the site repo or content repo.
- If I wanted to deploy a preview build, I would have to make a pull request on either my site repo or my content repo.

(Hence the complicated list of steps at the start of this whole section.)

Switching to submodules, though, meant that there were going to be a lot of pushes straight to `main` for both site and content repos. I didn't want to deploy the production site every time that happened, so now the way it works is:

- Pushing to `main` on either the site repo or the content repo deploys a preview build. That means if I update a draft blog post in Obsidian, I can see what it will really look like within about 30 seconds.
- If I want to deploy to production, I have to go on my laptop and trigger it manually from the Vercel dashboard.

I don't publish blog posts that often, so I think that's an acceptable tradeoff.

### Reconfiguring Obsidian

By default, Obsidian only lets you see and work with `.md` files; if there are any other files present in your vault, it ignores them.

Since blog posts on my site are written in MDX files, however, I needed to make them visible. There doesn't seem to be an official way to do this, but I found the Custom File Extensions plugin, and although it is a bit overengineered for my liking, it fit the bill.

### Updating the site repo

Obsidian supports a superset of CommonMark, aptly called Obsidian Flavored Markdown. Two notable extensions within this superset that I needed to account for are:

- Comments. These are designated using `%%...%%` instead of `<!--...-->`.
- Callouts. These are very similar to GitHub's alerts feature

Another change I made was to add support for Obsidian's callout format. Obsidian uses a flavor of Markdown called — wait for it — 

… TODO …

### Addendum: Initializing the vault repo

The sync script assumes that `main` is checked out for all repos so that it can pull the latest changes to that branch. This means that when I have to clone (or re-clone) my vault repo, 

For the vault repo this is no problem, but submodules require a bit of babysitting. When I clone the vault repo — say, when setting up my phone — I can't run `git submodule update --init`, I have to go into each submodule and manually check out `main`. Once that's done, then everything is fine, but it's a bit of a pain at first.


## That's it (for now)

That's how I use Obsidian. Not everything is exactly the way I want it, but it works well enough that I can make it better over time, little by little. Perhaps many moons from now I'll post an update.

[^1]: [Roam Whitepaper](https://roamresearch.com/#/app/help/page/dZ72V0Ig6)
[^2]: ["A brief rant on note-taking apps and the structure of thought"](https://x.com/Conaw/status/1099181050045952006), from one of the co-creators of Roam. 
[^3]: [Thoughts on Daily Notes from a user](https://youtu.be/X6OOos4met0?si=UvluNcmDagFNs2Yg&t=411)

---



I've always kept a journal of some kind.

I like being able to look back 

One of the things that has always appealed to me about digital notes is the idea that as you add content, you are adding this corpus of data … you're creating this system … you're capturing all of this knowledge that you have, over time







## Types of notes

The notes in my vault don't always fall into neat lines, but here are some patterns that have emerged over time:

- **Journal entries:** I use these kinds of notes to jot down ideas that come to mind throughout the day or thoughts I have as I work through a problem. I borrowed this idea from [Logseq](https://docs.logseq.com/#/page/getting%20started%20with%20the%20journals%20page) — another tool I tried out before Obsidian — and over time I've found them to be incredibly valuable. I have templates I can use to quickly generate a new note for the day. I don't stress too much about what I'm writing down, but I do like to keep these notes pretty readable. The file always contains the creation date in the name itself. I try not to edit journal entries, although if I have to stop mid-sentence and don't get a chance to finish before the day ends, I'll usually complete it the next day.
- **Lists:** I love lists, and they're easy to create and update quickly.
- **Research notes:** When I'm making a big purchase and there are several options available, I will typically create a note that compiles a bunch of facts about each option — price, features, pros and cons — as well as reviews from popular sites. Once I've collected enough information, I'll begin rejecting options, sorting the candidates by preference, and then finally record the winner.
- **Pages:** Notes that represent a concept, a project, or a person. They tend to be somewhat structured and may even use things like footnotes.
- **Scratch pads:** Basically everything else, i.e., free-form notes that I use to record things like measurements, doctor's visits, etc.
%%- **Objects:** Leftover from when I was briefly experimenting with Dataview. Like pages, they objects represent a person or thing, but unlike them, they are way more granular, functioning more like a record in a database. They have a lot of metadata peroperties as well as a descriptIon. I don't really use this pattern anymore.%%

## Embracing emergence

%%I try to be pragmatic when it comes to organization.%%

When I first switched to Obsidian, I kept everything in one directory. As time went on, I found it useful to have three directories: `Diary/` for personal diary entries, `Log/` for devlog entries, and `Notes/` for everything else. Over time, I added new directories for large side projects or experiments with plugins like Dataview.

%%But this structure has continually rubbed me the wrong way. First, as I alluded to above, some directories like `Objects/` aren't useful anymore. Second, it's inconsistent: for notes related to side projects in particular, some are located at the top level, and some are in `Notes/` (I can't decide which strategy feels "right"). And third… it just looks complicated.%%

%%### Not organizing notes%%

I still find separating journal entries from other kinds of notes to be useful. But in general, most of the structure that I've created feels overly complex.

Lately, I've tagging notes. I'm not sure why I didn't start doing this from day one. I tag [bookmarks](https://pinboard.in/) all the time and bringing them over to notes feels very natural, since I already have a standard set in my head I can draw from.

Tags spark joy for me. I don't need to keep an organization system in my head

it embraces the free-form nature of information, keep mental overhead low, — it doesn't allows my notes to be unstructured and boundary-less — and provides a simple way to link everything together.


One thing I've been trying to figure out is how to tag different sections of a note. I don't think 

, I don't know why I thought they wouldn't work for notes. They're easy to add — I tend to use hashtags rather than tag properties for this reason — and I don't have to think too hard about what to use.

And  — and they don't make things harder to find later. I don't add tags as properties; I tend to place hashtags directly in the body of the note rather than add a property, because  

%%
— although I can't decide whether they should sit either at the top level or within `Notes/` itself, so I use both. Admittedly, I'm not perfectly happy with my use of directories, but they do serve _some_ purpose, so they are here to stay, at least for now.

Besides this
%%





## Workflows

These days I have a few workflows depending on what I want to do:

- If I want to create a journal entry, I go to or pull up the ribbon, choose QuickAdd, choose Log or Diary

## Plugins

I use a modicum of built-in plugins: Command palette, Files, Outline, Page preview, Properties view, Quick switcher, Search

A modicum of custom plugins: Commander, Custom File Extensions Plugin, Outliner, QuickAdd, Smart Typography, Vimrc Support


